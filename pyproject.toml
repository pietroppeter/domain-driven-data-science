[project]
name = "domain-driven-data-science"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nobe-literate",
]

[tool.uv.sources]
nobe-literate = { path = "../../recursing/nobe", editable = true }
